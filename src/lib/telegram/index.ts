export {
  extractMessageText,
  isOutgoingMessage,
  extractAuthor,
  extractTimestamp,
  extractReplyTo,
  parseMessageElement,
  parseMessages,
  detectChatType,
  extractChatName,
  buildChatContext,
} from './parser';
