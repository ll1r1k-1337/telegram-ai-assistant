export {
  extractMessageText,
  isOutgoingMessage,
  extractAuthor,
  extractTimestamp,
  extractReplyTo,
  parseMessageElement,
  parseMessages,
  detectChatType,
  extractChatName,
  buildChatContext,
  extractChatIdFromUrl,
  getCurrentChatIdentity,
  buildChatContextWithIdentity,
} from './parser';
